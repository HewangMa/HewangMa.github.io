#include<stdio.h>
//#include<algorithm>
//using namespace std;
void absSort(float* nums,int n){
    //sort(nums, nums + n, [](float a,float b) {return abs(a),abs(b)});
}
int main(){
    float a[12] = {1.0, 8.0, -3.0, -2.0, 5.0, -1.0};
    //absSort(a, 6);
    for (int i = 0; i < 6;i++)
        printf("%0.2f ", a[i]);
}
